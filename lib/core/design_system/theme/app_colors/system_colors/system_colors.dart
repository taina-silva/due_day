import 'package:flutter/material.dart';

class SystemColors {
  const SystemColors();

  Color get success => const Color(0xFF13C999);
  Color get error => const Color(0xFFFF6363);
  Color get warning => const Color(0xFFFFB74D);
  Color get info =>
      const Color(0xFF456EFE); // Same as primary per docs typically
}
